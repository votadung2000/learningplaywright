let a = 1;
let b = 2;

// Hints: ++ -> increase
let c = ++ a + b ++;

/**
 * a = ?
 * b =?
 * c =?
 * console.log(`a  = ${a}, b = ${b}, c= ${c}`);
 * prefix > right operand > assignment > postfix
 * a = 2
 * c = 4
 * b = 3
 */

console.log(`a  = ${a}, b = ${b}, c= ${c}`);